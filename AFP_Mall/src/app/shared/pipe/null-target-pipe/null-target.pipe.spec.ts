import { NullTargetPipe } from './null-target.pipe';

describe('NullTargetPipe', () => {
  it('create an instance', () => {
    const pipe = new NullTargetPipe();
    expect(pipe).toBeTruthy();
  });
});
