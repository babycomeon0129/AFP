import { KeyControllerDirective } from './key-controller.directive';

describe('KeyControllerDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyControllerDirective();
    expect(directive).toBeTruthy();
  });
});
