import { TargetControllerDirective } from './target-controller.directive';

describe('TargetControllerDirective', () => {
  it('should create an instance', () => {
    const directive = new TargetControllerDirective();
    expect(directive).toBeTruthy();
  });
});
